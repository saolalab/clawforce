name: Release Zalo Personal Bridge

on:
  push:
    tags:
      - "bridge/zalo/v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g. 0.1.1)"
        required: true
        type: string

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.set.outputs.version }}
      ref_to_use: ${{ steps.ref.outputs.ref_to_use }}
    steps:
      - id: set
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "version=${{ inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/bridge/zalo/v}" >> $GITHUB_OUTPUT
          fi

      - id: ref
        run: echo "ref_to_use=${{ github.ref }}" >> $GITHUB_OUTPUT

  build:
    runs-on: ubuntu-latest
    needs: version
    defaults:
      run:
        working-directory: bridges/zalo

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.version.outputs.ref_to_use }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Update package version
        run: npm version ${{ needs.version.outputs.version }} --no-git-tag-version --allow-same-version

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Pack tarball
        run: npm pack

      - name: Upload tarball
        uses: actions/upload-artifact@v4
        with:
          name: zalo-bridge-tarball
          path: bridges/zalo/clawbot-zalo-personal-bridge-*.tgz

  github-release:
    runs-on: ubuntu-latest
    needs: [version, build]
    permissions:
      contents: write

    steps:
      - name: Download tarball
        uses: actions/download-artifact@v4
        with:
          name: zalo-bridge-tarball
          path: dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: bridge/zalo/v${{ needs.version.outputs.version }}
          name: "Zalo Personal Bridge v${{ needs.version.outputs.version }}"
          generate_release_notes: false
          draft: false
          files: dist/*.tgz
          body: |
            ## @clawbot/zalo-personal-bridge v${{ needs.version.outputs.version }}

            Install directly from this release (no npm registry required):

            ```bash
            npm install https://github.com/saolalab/clawforce/releases/download/bridge%2Fzalo%2Fv${{ needs.version.outputs.version }}/clawbot-zalo-personal-bridge-${{ needs.version.outputs.version }}.tgz
            ```

            Or pin it in `package.json`:

            ```json
            "@clawbot/zalo-personal-bridge": "https://github.com/saolalab/clawforce/releases/download/bridge%2Fzalo%2Fv${{ needs.version.outputs.version }}/clawbot-zalo-personal-bridge-${{ needs.version.outputs.version }}.tgz"
            ```
